[pytest]
# Put the repo root on sys.path so `import app.main` resolves under bare `pytest`
# (CI runs `pytest`, not `python -m pytest`, so the cwd is not auto-added).
pythonpath = .
testpaths = tests
